import { describe, it } from 'mocha'
import { expect } from 'chai'

import { createStore } from '../../src/store'

describe('store', () => {

  it('1 + 2 equals 3', () => {
    expect(1 + 2).to.eql(3)
  })

})
