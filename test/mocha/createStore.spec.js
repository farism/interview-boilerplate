import { describe, it } from 'mocha'
import { expect } from 'chai'

import { createStore } from '../../src/store'

describe('createStore', () => {

  it('accepts a reducer and an initial state as arguments', () => {

  })

  it('returns an object with a `getState` helper function', () => {

  })

  it('`getState` helper function returns the current state of the store', () => {

  })

  it('returns an object with a `getSubscribers` helper function', () => {

  })

  it('`getSubscribers` current subscribers to the store', () => {

  })

  it('returns an object with a `dispatch` helper function', () => {

  })

  it('initial state is equal to the state passed in as arg', () => {

  })

  it('`dispatch` of an increment action increases the value of the counter state', () => {

  })

  it('`dispatch` of a decrement action decreases the value of the counter state', () => {

  })

})
