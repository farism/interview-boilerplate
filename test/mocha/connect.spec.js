import { describe, it } from 'mocha'
import { expect } from 'chai'

import connect from '../../src/connect'

describe('connect', () => {

  it('1 + 2 equals 3', () => {
    expect(1 + 2).to.eql(3)
  })

})
